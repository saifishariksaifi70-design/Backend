Node.js
Task Name: Build a Multi-Route Node.js Server

Objective:
Extend the basic Node.js HTTP server to handle different URL routes, set appropriate headers, and send dynamic HTML and JSON responses.

Task Requirements:

Route 1 (/ - Home Page):

Serve HTML content with Content-Type: text/html.

Display an <h1> heading: "Welcome to Node.js Server".

Route 2 (/about - About Page):

Serve an HTML page containing an <h2> title and a short paragraph with the student's name and roll number.

Route 3 (/api/student - API Endpoint):

Set Content-Type: application/json.

Send a JSON response with student details (e.g., { "name": "Alex", "course": "Full Stack", "status": "Enrolled" }).


Route 4 (404 Page / Any Unhandled Route):

Return an HTTP status code of 404.

Serve HTML displaying a red heading: "<h1 style='color: red;'>404 - Page Not Found</h1>".

-----------------------class Task ---------------------------
1-  Basic Task (Custom File Cloner & Chunk Monitor)

- Setup: Create a file named source.txt and populate it with 5–10 lines of sample text.

- Requirements:

   - Read source.txt using fs.createReadStream (set highWaterMark: 64 to force small chunks for testing).

       sol - const readStream  = fs.createReadStream('source.txt',{highWaterMark:64})

   - Write the incoming data to destination.txt using fs.createWriteStream.
        sol -  const writeStream  = fs.createWriteStream('destination.txt')
  

   - Listen to the data event and log the chunk sequence and size: Received chunk: <length in bytes>.
      sol = 
           let chunksCount =  0;

           readStream.on('data' , (chunk)=>{
            chunksCount++
           
            console.log(`Received Chunks ${chunksCount}: ${chunk.length} bytes`)
           })

   - Handle error events on both read and write streams.

   - Listen to the finish or end event and log: File successfully cloned!.



   -----------------------------new Task  ---------------
   Task 1: Chunk-Based Word & Character Counter


•	Setup: Create a file named story.txt with 2–3 paragraphs of sample text.

•	Requirements:


o	Read story.txt using fs.createReadStream with highWaterMark: 32.

o	In the data event, convert each chunk to a string and increment total Chunk Count, Character Count, and Word Count.

o	On the end event, log: Total Chunks: X | Total Characters: Y | Total Words: Z.

o	Include an error listener for the read stream.

Task 2: Manual Uppercase File Transformer


•	Setup: Create a file named lowercase.txt with all lowercase sentences.

•	Requirements:


o	Read lowercase.txt using fs.createReadStream (highWaterMark: 50).

o	Create a write stream targeting uppercase.txt.

o	Without using .pipe(), transform each incoming chunk inside the data event using .toString().toUpperCase() and pass it to writeStream.write().

o	Trigger writeStream.end() when the read stream fires the end event.

o	Listen to the finish event on the write stream and log: Text converted to uppercase successfully!.

Task 3: Real-Time Stream Progress Tracker


•	Setup: Create a file named data_input.txt (a medium-sized text file, ~100KB to 500KB).

•	Requirements:


o	Get the total file size in bytes using fs.statSync("data_input.txt").size.

o	Create a read stream with highWaterMark: 1024 (1 KB chunks).

o	Accumulate read bytes in the data event and log the percentage: Progress: XX.XX% completed.

o	Pipe the data into data_output.txt.

o	Log Transfer Complete! once the destination stream finishes.

Task 4: Rate-Limited Stream Reader (Pause & Resume)


•	Setup: Create a file named messages.txt with 10 lines of text.

•	Requirements:


o	Read messages.txt with highWaterMark: 25.

o	Inside the data event, immediately call readStream.pause() to stop reading.

o	Use setTimeout (1-second delay) to print the chunk to the console and then call readStream.resume().

o	On the end event, log: All chunks processed with delay!.

o	Handle read stream errors gracefully.

Task 5: Line-Numbered File Cloner


•	Setup: Create a file named notes.txt containing 5–8 distinct lines.

•	Requirements:


o	Read notes.txt and write to numbered_notes.txt.

o	Split incoming chunks into lines and prepend a dynamic line number to each line (e.g., 1: First line, 2: Second line).

o	Ensure trailing newlines are handled correctly across chunks.

o	If notes.txt does not exist, capture the error with .on('error') and log: Source file missing, process aborted!.

o	Log Indexing complete! on stream completion.